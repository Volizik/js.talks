const LinkedList1 = require('./LinkedList1');

const params = '';
const result = '';

describe('LinkedList', () => {
    test('Testing returned value 1', () => {
        expect(LinkedList1(params)).toEqual(result);
    })
});
